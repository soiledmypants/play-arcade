{
  "name": "play-site",
  "version": "4.0.0",
  "private": true,
  "description": "Free-play computer arcade — guest FIFO queue + agent-computer stream (static + Python stdlib API)",
  "scripts": {
    "start": "python3 server.py",
    "dev": "python3 server.py"
  },
  "engines": {
    "node": ">=18"
  }
}
